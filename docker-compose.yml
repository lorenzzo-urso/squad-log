services:
  app:
    build: .
    ports:
      - "8000:8000"
    environment:
      ADMIN_EMAIL: ${ADMIN_EMAIL}
      ADMIN_PASSWORD: ${ADMIN_PASSWORD}
    volumes:
      - db:/data/db
      - uploads:/data/uploads
    restart: unless-stopped

volumes:
  db:
  uploads:
